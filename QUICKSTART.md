# 🚀 Быстрый запуск PriceForecastingSystem

### 🎯 Краткая версия
1. Запустите `.\run-server-manual.bat`
2. Откройте `docs\START.html` в браузере
3. Следуйте инструкциям на странице

## 📋 Содержание
- [Запуск сервера](#запуск-сервера)
- [Остановка сервера](#остановка-сервера)
- [Использование приложения](#использование-приложения)
- [Тестовые данные](#тестовые-данные)
- [API документация](#api-документация)

## 🖥️ Запуск сервера

### Способ 1: Автоматический .bat файл (самый простой)
```bash
# Дважды щелкните по файлу в проводнике или запустите:
.\run-server-manual.bat
```

### Способ 2: PowerShell скрипт (рекомендуется для разработчиков)
```bash
# Из корневой папки проекта запустите:
.\start-server.ps1

# Или через .bat файл:
.\start-server.bat
```

### Способ 3: Ручной запуск
```bash
# 1. Проверить и завершить старые процессы
.\stop-server.bat

# 2. Перейти в папку API
cd "PriceForecasting.API\PriceForecasting.API"

# 3. Запустить сервер
dotnet run
```

## 🛑 Остановка сервера

```bash
# Автоматическая остановка всех процессов
.\stop-server.ps1
```

Или нажмите `Ctrl+C` в окне терминала с сервером.

## 🌐 Использование приложения

### 📊 Swagger UI (тестирование API)
```
http://localhost:5229/swagger
```

### 🎨 Docs демо (главный интерфейс)

**Способ 1: Удобная стартовая страница (рекомендуется)**
```
docs\START.html
```
Откройте для пошаговых инструкций

**Способ 2: Локальный веб-сервер**
```bash
.\serve-docs.bat
```
Затем откройте: `http://localhost:8000`

**Способ 3: Прямое открытие файла (все пути исправлены)**
```
file:///C:/Users/Пользователь/source/repos/PriceForecastingSystem/docs/index.html
```
⚠️ **Важно:** API сервер должен быть запущен!

### 💼 Frontend приложение
```
file:///C:/Users/Пользователь/source/repos/PriceForecastingSystem/PriceForecasting.Frontend/index.html
```
⚠️ **Важно:** Открывайте файл в браузере, API сервер должен быть запущен!

## 🧪 Тестовые данные

### Артикулы товаров:
- `482159736` - iPhone 15 128GB
- `5938472610` - iPhone 15 256GB
- `620184735` - Samsung Galaxy S24
- `815937402` - Xiaomi 13 Lite
- `9374851260` - AirPods Pro 2

### Категории:
- 1: Смартфоны
- 2: Ноутбуки
- 3: Наушники
- 4: Бытовая техника

## 📚 API документация

### Основные эндпоинты:

```bash
# Получить все товары
GET /api/products

# Получить цену товара по артикулу
GET /api/price/demo/{article}

# Получить ML рекомендации
GET /api/recommendations/{article}?period=30&scenario=optimist

# Получить прогноз цен
GET /api/forecast/{article}?days=7&scenario=optimist

# Получить категории
GET /api/categories
```

### Регистрация и вход:

```bash
# Регистрация
POST /Auth/register
{
  "Email": "user@example.com",
  "Password": "password123"
}

# Вход
POST /Auth/login
{
  "Email": "user@example.com",
  "Password": "password123"
}
```

## 🔧 Возможные проблемы

### ❌ "Не удается загрузить файл ... .ps1, так как выполнение скриптов отключено"
**Решение:**
```bash
# Откройте PowerShell от имени администратора и выполните:
Set-ExecutionPolicy RemoteSigned

# Или используйте .bat файлы вместо .ps1:
.\run-server-manual.bat
.\start-server.bat
.\stop-server.bat
```

### Порт 5229 занят
```bash
# Проверить занят ли порт
netstat -ano | findstr ":5229"

# Завершить процесс (замените XXXX на PID)
taskkill /PID XXXX /F
```

### База данных не создана
Сервер автоматически создаст базу данных при первом запуске с тестовыми данными.

### ML сервис недоступен
Если Python ML сервис недоступен, система использует mock данные с предупреждением.

## 🎯 Быстрый тест

```bash
# Запуск сервера
.\start-server.ps1

# В новом окне терминала:
curl http://localhost:5229/api/products
curl "http://localhost:5229/api/price/demo/482159736"
```

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте запущенные процессы: `tasklist | findstr "PriceForecasting"`
2. Завершите старые процессы: `.\stop-server.ps1`
3. Перезапустите сервер: `.\start-server.ps1`
4. Проверьте логи в терминале

---

**Приятного использования! 🚀**
